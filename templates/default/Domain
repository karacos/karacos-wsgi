
<div class="main-content">
 
	% if 'content' in instance:
		<h1 class="block">
		% if 'title' in instance:
        ${instance['title']}
		% else:
			Bienvenue sur le domaine ${instance['name']}
		% endif
		</h1>
        <div class="column1-unit">
        <p>${instance['content']}</p>
        <p class="details">| 
        % if 'owner' in instance:
        	Owner : <a href="#">${instance['owner']}</a> | 
        %endif
        Categories: <a href="#">General</a> | Comments: <a href="#">73</a> |</p>
          
        </div>
	% endif
	</div>
	
	
